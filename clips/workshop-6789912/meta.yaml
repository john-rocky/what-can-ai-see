# Fetched by tools/fetch_scene.py — searched by PLACE, not by event.
id: workshop-6789912
tier: field
event: object-removed
label: rejected
pair: null
onset_s: 4.5
duration_s: 15.0
camera: fixed
ground_truth: A fixed overhead camera on a workbench laid out with tools. The arrangement is stable,
  then at 11.5s an item is taken away and the space it occupied is left empty. Further items are added
  and removed later (edges at 13.1s, 18.6s, 19.8s, 21.6s).
source:
  kind: stock
  provider: pexels
  id: 6789912
  url: https://www.pexels.com/video/carpentry-flatlay-6789912/
  title: carpentry flatlay
  scene_query: workshop
  license: pexels
  license_note: free to use, no attribution required, redistribution of the file itself is not permitted
    — the benchmark ships ids and urls, not the mp4s.
conditions:
  viewpoint: fixed-overhead
  distance: near
  light: indoor-even
  occlusion: none
  blur: none
  clutter: high
selection: kept by tools/review.py after tools/enter_onset.py measured the camera as fixed. 165 of
  327 candidates were rejected on camera motion alone, which a 6-frame contact sheet does not reveal.
trim:
- 7.0
- 22.0
rejected: enter_onset.py finds six edges, but the bench looks the same at 0.5s and at 14s and I cannot
  name which tool left. The measurement is detecting a slow zoom and small relighting, not a removal.
  A clip a person cannot call is a clip where a model's failure measures the harness.
